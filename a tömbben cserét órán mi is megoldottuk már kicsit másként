/*
A saját programunk példányváltozóit, paramétereit, metódusneveket átírtam, hogy ugyanazok legyenek, mint az előző megoldásban
Mint látható, nálunk a konstruktor csak három paramétert vár
A miénkben csak egy metódus tér vissza értékkel, amásik kettő eljárás típusú. 

Meghívása a main metódusból:
Csere abCsere = new Csere(20, 'A', 'B');
*/


public class Csere {
    private char karakterek[];    

public Csere(int hossz, char mit, char mire) {
        karakterek = new char[hossz];
        feltolt(hossz, mit);
        kiir(karakterek);
        karakterek=lecserel(karakterek, mit, mire);
        kiir(karakterek);
    }
    private void feltolt(int h, char mivel) {
        for (int i = 0; i < h; i++) 
            karakterek[i] = mivel;
    }
    private void kiir(char[] tomb) {
        for (int i = 0; i < tomb.length; i++) 
            System.out.print(tomb[i] + " ");
        System.out.println();
    }
    private char[] lecserel(char[] tomb, char mit, char mire) {
        for (int i = 0; i < tomb.length; i++) 
            if (i % 2 != 0) 
                tomb[i] = mire;
        return tomb;
    }
}

